algoritmo "Aula 9 – Estruturas de Repetição 1 - Contagem Regressiva"
var
   Contador: Inteiro
inicio
   Escreval ("CONTAGEM REGRESSIVA")
   Contador <- 10
   Enquanto (Contador >= 0) faca
      Escreval (Contador)
      Contador <- (Contador - 1)
   FimEnquanto
   Escreval ("FIM DA CONTAGEM REGRESSIVA")
fimalgoritmo